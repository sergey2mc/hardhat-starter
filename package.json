{
  "name": "hardhat-starter",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "yarn blockchain",
    "hardhat:compile": "yarn blockchain compile",
    "hardhat:test": "yarn blockchain test",
    "hardhat:lint": "yarn blockchain lint",
    "hardhat:watch": "yarn blockchain watch",
    "hardhat:deploy:rinkeby": "yarn blockchain deploy:rinkeby",
    "hardhat:verify:rinkeby": "yarn blockchain verify:rinkeby",
    "blockchain": "yarn --cwd blockchain"
  }
}
